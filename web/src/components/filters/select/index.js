import angular from 'angular'

import { FilterSelectComponent } from './select.component'

export default angular
  .module('filterSelect', [])
  .component('filterSelect', FilterSelectComponent)
  .name
